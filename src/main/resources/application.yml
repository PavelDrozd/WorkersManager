server:
  port: 8080
  servlet:
    session:
      timeout: 30m
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql:3306/workersmanager
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none